{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Debug of Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/tests/test.cfg",
        "${workspaceFolder}/tests/positive_negative.cfg"
      ]
    }
  ]
}